# Book Rating System - Курсова робота з Баз Даних

Система оцінювання книг з можливістю залишати рейтинги та відгуки.

---

## 👥 Contributures

**Назва:** Book Rating System  
**Дисципліна:** Бази даних  
**Автор:** Клеценко Оксана
**Група:** ІМ-42

---

## 🎯 Опис проєкту

Book Rating System - це backend API для системи оцінювання книг, яка дозволяє:

- ✅ Реєструвати користувачів
- ✅ Додавати книги з інформацією про авторів, жанри та видавництва
- ✅ Залишати оцінки (1.0-5.0) та текстові відгуки
- ✅ Переглядати статистику активності користувачів
- ✅ Аналізувати рейтинги книг
- ✅ Керувати списками читання

### Предметна область

Система розроблена для бібліотек, книгарень та онлайн-платформ читання, де користувачі можуть:

- Відслідковувати прочитані книги
- Ділитися враженнями від прочитаного
- Отримувати рекомендації на основі рейтингів
- Аналізувати популярність книг та авторів

---

## 🛠 Технологічний стек

### Backend

- **Framework:** Express.js 5.2.1
- **Runtime:** Node.js
- **Мова:** JavaScript (CommonJS)

### База даних

- **СУБД:** PostgreSQL 16 (Alpine)
- **ORM:** Prisma 6.19.1
- **Драйвер:** pg 8.16.3

### Тестування

- **Framework:** Jest 30.2.0
- **HTTP Testing:** Supertest 7.0.0

### Контейнеризація

- **Docker:** PostgreSQL контейнер
- **Docker Compose:** Оркестрація сервісів

### Інші інструменти

- **CORS:** cors 2.8.5 (для фронтенду)
- **Prisma Studio:** Візуальний редактор БД

## 📋 Встановлення та запуск

### Передумови

Переконайся, що на твоєму комп'ютері встановлено:

- Node.js (версія 18+)
- Docker та Docker Compose
- Git

### 1. Клонування репозиторію

```bash
git clone https://github.com/seniaz/DB_course_work.git
cd DB_course_work
```

### 2. Встановлення залежностей

```bash
npm install
```

### 3. Налаштування змінних середовища

Створи файл `.env` в корені проєкту:

```env
DATABASE_URL="postgresql://postgres:labsdatabase@localhost:5433/book_rating?schema=public"
PORT=3000
NODE_ENV=development
```

### 4. Запуск бази даних

```bash
docker-compose up -d db
```

Перевірка статусу:

```bash
docker-compose ps
```

### 5. Застосування міграцій

```bash
npx prisma migrate dev
npx prisma generate
```

### 6. Наповнення бази даних тестовими даними

```bash
npm run prisma:seed
```

Ти побачиш:

```
🌱 Starting database seeding...
✅ Database cleared
✅ Sequences reset to 1
✅ Publishers created: 3
✅ Authors created: 5
✅ Genres created: 5
✅ Books created: 5
✅ Users created: 3
✅ Ratings created: 6
✅ Reviews created: 3
🎉 Database seeding completed!
```

### 7. Запуск сервера

**Режим розробки (з автоматичним перезапуском):**

```bash
npm run dev
```

**Звичайний запуск:**

```bash
npm start
```

Сервер буде доступний за адресою: **http://localhost:3000**

### 8. Перевірка роботи

Відкрий у браузері:

- **Фронтенд:** http://localhost:3000
- **Health check:** http://localhost:3000/health
- **Prisma Studio:** `npx prisma studio` (відкриється на http://localhost:5555)

---

## 🧪 Тестування

### Запуск всіх тестів

```bash
npm test
```

### Запуск конкретного тесту

```bash
npm test tests/task1_creation.test.js
npm test tests/task2_update.test.js
npm test tests/task3_delete.test.js
```

### Режим watch (автоматичний перезапуск при змінах)

```bash
npm run test:watch
```

### Очікуваний результат

```
PASS  tests/task1_creation.test.js
  ✓ should create rating AND review (150ms)
  ✓ should return 409 on duplicate (45ms)

PASS  tests/task2_update.test.js
  ✓ should update with correct timestamp (120ms)
  ✓ should return 409 with old timestamp (50ms)

PASS  tests/task3_delete.test.js
  ✓ should soft delete user and their data (180ms)

Test Suites: 3 passed, 3 total
Tests:       5 passed, 5 total
```

---

## 📁 Структура проєкту

```
book-rating-system/
│
├── .env                        # Змінні середовища
├── .gitignore                  # Git ignore файл
├── docker-compose.yml          # Конфігурація Docker
├── Dockerfile                  # Docker image додатку
├── jest.config.js              # Налаштування Jest
├── package.json                # Залежності проєкту
├── README.md                   # Документація (цей файл)
│
├── docs/                       # Додаткова документація
│   ├── queries.md              # Опис SQL запитів
│   └── schema.md               # Структура бази даних
│
├── prisma/                     # Prisma ORM
│   ├── schema.prisma           # Схема бази даних
│   ├── seed.js                 # Скрипт наповнення даними
│   └── migrations/             # Історія міграцій
│
├── public/                     # Статичні файли (фронтенд)
│   └── index.html              # Веб-інтерфейс для демонстрації
│
├── src/                        # Вихідний код
│   ├── db.js                   # Підключення до БД
│   ├── index.js                # Точка входу додатку
│   └── routes/                 # API маршрути
│       ├── analytics.js        # Аналітичні запити (Завдання 5)
│       ├── ratings.js          # Рейтинги (Завдання 2, 4)
│       ├── reviews.js          # Відгуки (Завдання 1)
│       └── users.js            # Користувачі (Завдання 3)
│
└── tests/                      # Тести
    ├── task1_creation.test.js  # Тести Завдання 1
    ├── task2_update.test.js    # Тести Завдання 2
    └── task3_delete.test.js    # Тести Завдання 3
```

---

## 📊 Схема бази даних

База даних знаходиться в **3НФ (Третій Нормальній Формі)** і включає:

### Основні таблиці:

- **Users** - Користувачі системи
- **Book** - Книги
- **Author** - Автори
- **Publisher** - Видавництва
- **Genre** - Жанри

### Зв'язкові таблиці:

- **Rating** - Оцінки книг користувачами
- **Review** - Текстові відгуки
- **BookAuthor** - Зв'язок книг з авторами (M:N)
- **BookGenre** - Зв'язок книг з жанрами (M:N)
- **ReadingList** - Списки читання користувачів

### Ключові особливості:

- ✅ Первинні ключі (PRIMARY KEY) у всіх таблицях
- ✅ Зовнішні ключі (FOREIGN KEY) для зв'язків
- ✅ CHECK constraints для валідації даних
- ✅ UNIQUE constraints для унікальності
- ✅ Індекси для оптимізації запитів
- ✅ Soft Delete (поле `deletedAt`)
- ✅ Timestamps (`createdAt`, `updatedAt`)

Детальний опис схеми знаходиться у файлі `docs/schema.md`.

---

## 🔧 Корисні команди

### Prisma

```bash
# Відкрити Prisma Studio (візуальний редактор)
npx prisma studio

# Застосувати міграції
npx prisma migrate dev

# Створити нову міграцію
npx prisma migrate dev --name название_миграции

# Згенерувати Prisma Client
npx prisma generate

# Скинути БД (видалить всі дані!)
npx prisma migrate reset

# Перезапустити seed
npm run prisma:seed
```

### Docker

```bash
# Запустити БД
docker-compose up -d db

# Зупинити БД
docker-compose stop db

# Видалити БД з даними
docker-compose down -v

# Переглянути логи БД
docker-compose logs db

# Перевірити статус контейнерів
docker-compose ps
```

### Розробка

```bash
# Запуск з автоматичним перезапуском
npm run dev

# Звичайний запуск
npm start

# Тести
npm test

# Тести з coverage
npm test -- --coverage
```

## 📝 Додаткова документація

Детальна документація знаходиться в папці `docs/`:

- **`docs/schema.md`** - Структура бази даних, ER-діаграма, опис таблиць
- **`docs/queries.md`** - Опис SQL запитів, бізнес-логіка, приклади
